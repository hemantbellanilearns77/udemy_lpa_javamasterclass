package com.hb.study.udemy_lpa_javamasterclass.global.libs.datetimeutillib.core;

/**
 * created by : heman on 14-07-2025, 08:41 pm, in the "udemy_lpa_javamasterclass" project
 **/
public enum FormatterMode {
    MINIMAL_DEMO_MODE,
    PATTERN_DEMO_FIESTA_MODE,
    CURRENCY_AND_NUMBERS_DEMO_MODE,
    INDIAN_LANGUAGE_PATTERNS_MODE,
    GLOBAL_LOCALES_PATTERNS_MODE,
    TRADITIONAL_CALENDARS_MODE,
    FORMAT_STYLES_COMBOS_DEMO_MODE,
    VEDIC_MODE,
    FULL_CARNIVAL_BLAST
}

/*
 DTFormatterUtils.runMinimalDemo(locale, zone);
                DTFormatterUtils.runPatternFiesta();
                DTFormatterUtils.runTraditionalCalendars();
                DTFormatterUtils.runCurrencyAndNumbers();
                DTFormatterUtils.runFormatStyleCombos(locale, zone);
                FormatterGUI.showVedicPlaceholder();
* */
